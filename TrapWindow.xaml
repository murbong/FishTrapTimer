<Window x:Class="FishTrapTimer.TrapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FishTrapTimer"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Title="TrapWindow" Height="900" Width="1600"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated"
        SizeChanged="Window_SizeChanged"
        >
    <Grid>
        <Canvas x:Name="canvas"/>
        <Rectangle x:Name="Rect_Outline" VerticalAlignment="Stretch" Fill="Transparent" Stroke="LightBlue" MouseLeftButtonDown="Rectangle_MouseLeftButtonDown"/>

    </Grid>
</Window>
